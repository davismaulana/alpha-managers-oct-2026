services:
  # Dokploy domain binding for oct-2026.zenova.id targets this service name.
  oct-2026-web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"

networks:
  dokploy-network:
    external: true
